serviceName: checkoutservice
replicas: 5
image: ghcr.io/thuanthentran/checkoutservice:latest # Đảm bảo tag image đồng bộ với CI/CD
containerPort: 5050

# ĐÃ XÓA: podAnnotations liên quan đến Istio

# Ép Pod phải Ready hoàn toàn mới cho request vào
readinessProbe:
  tcpSocket:
    port: 5050
  initialDelaySeconds: 5
  periodSeconds: 3
  failureThreshold: 2 

# Lệnh hành quyết: Nếu Pod bị treo, giết và tạo lại
livenessProbe:
  tcpSocket:
    port: 5050
  initialDelaySeconds: 10
  periodSeconds: 5
  failureThreshold: 3

env:
  - name: PORT
    value: "5050"
  - name: FAULT_SCENARIO
    value: "none" # Luôn giữ none, Agent sẽ tự tiêm lỗi qua biến môi trường sau
  - name: PRODUCT_CATALOG_SERVICE_ADDR
    value: "cilium-gateway-productcatalogservice-gateway:80"
  - name: SHIPPING_SERVICE_ADDR
    value: "cilium-gateway-shippingservice-gateway:80"
  - name: PAYMENT_SERVICE_ADDR
    value: "cilium-gateway-paymentservice-gateway:80"
  - name: EMAIL_SERVICE_ADDR
    value: "cilium-gateway-emailservice-gateway:80"
  - name: CURRENCY_SERVICE_ADDR
    value: "cilium-gateway-currencyservice-gateway:80"
  - name: CART_SERVICE_ADDR
    value: "cilium-gateway-cartservice-gateway:80"