# gitops/charts/bootstrap/values.yaml
global:
  repoURL: 'https://github.com/thuanthentran/dqn-lstm-agent-driven-canary-release'
  targetRevision: OffTrain
  destinationNamespace: default

services:
  # Chỉ giữ lại 3 thành phần thiết yếu
  - name: frontend
    chartPath: gitops/charts/universal-canary
    valueFile: ../../releases/frontend-values.yaml
    
  - name: checkoutservice
    chartPath: gitops/charts/universal-canary
    valueFile: ../../releases/checkoutservice-values.yaml
    
  - name: loadgenerator
    chartPath: gitops/charts/loadgenerator
    valueFile: ../../releases/loadgenerator-values.yaml

  - name: base-resources
    chartPath: gitops/base
