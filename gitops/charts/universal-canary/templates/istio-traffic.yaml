{{- if not .Values.isLoadGenerator }}
# =========================================================
# Istio VirtualService
# Argo Rollouts will automatically adjust stable/canary weights during rollout.
# retries.attempts=0 disables Envoy auto-retry so the RL agent sees raw
# failure signals instead of masked/healed errors.
# =========================================================
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ .Values.serviceName }}-vsvc
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  hosts:
  - {{ .Values.serviceName }}
  http:
  - retries:
      attempts: 0         # Disable Envoy retry — RL agent must see raw errors
    timeout: 5s           # Hard timeout per request
    route:
    - destination:
        host: {{ .Values.serviceName }}
        subset: stable
      weight: 100
    - destination:
        host: {{ .Values.serviceName }}
        subset: canary
      weight: 0
---
# =========================================================
# Istio DestinationRule
# Defines stable and canary subsets; Argo Rollouts injects
# pod-template-hash labels at runtime to pin traffic.
# =========================================================
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: {{ .Values.serviceName }}-destrule
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  host: {{ .Values.serviceName }}
  subsets:
  - name: stable
    labels:
      app: {{ .Values.serviceName }}
  - name: canary
    labels:
      app: {{ .Values.serviceName }}
{{- end }}
