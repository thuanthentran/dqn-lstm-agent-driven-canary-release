# Setup môi trường cho KWOK
1. Chạy script create-cluster.sh. Script này chỉ chạy được khi đã cài KWOK và kubectl. 
2. Để chạy được cần cấp quyền thực thi:
```bash
chmod +x simulation/kwok/setup/create_cluster.sh
```
